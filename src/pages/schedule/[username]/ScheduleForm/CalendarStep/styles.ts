import { Box, styled } from '@ignite-ui/react'

export const Container = styled(Box, {
  display: 'grid',
  margin: '$6 auto 0',
  padding: 0,
  maxWidth: '100%',
  position: 'relative',
  width: 540,
  gridTemplateColumns: '1fr',
})
